DATABASE_URL=

# =============================================================================
# MODEL TRAINING CONFIGURATION
# =============================================================================
# Options: "both", "rf", "xgb"
#   - both: Train Random Forest AND XGBoost, select best automatically
#   - rf:   Train only Random Forest
#   - xgb:  Train only XGBoost
MODEL_TYPE=both

# =============================================================================
# HYPERPARAMETERS (Optional - defaults shown)
# =============================================================================
# Random Forest
RF_N_ESTIMATORS=300
RF_MAX_DEPTH=6
RF_MIN_SAMPLES_SPLIT=200
RF_MIN_SAMPLES_LEAF=50

# XGBoost
XGB_N_ESTIMATORS=300
XGB_MAX_DEPTH=4
XGB_LEARNING_RATE=0.05
XGB_SUBSAMPLE=0.8
XGB_COLSAMPLE_BYTREE=0.8

# =============================================================================
# BACKTEST CONFIGURATION
# =============================================================================
INITIAL_CAPITAL=10000
RISK_PER_TRADE=0.02
TRADING_FEE=0.0005